{
  "name": "journalist-selection-review",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "领域事件基础校验",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node --check src/validator.js && for f in src/*.js; do node --check \"$f\"; done"
  }
}
